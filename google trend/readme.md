# 🌍 글로벌 F&B 시장 및 Google Trends 분석 시스템 (`app/google trend`)

OpenAI API, Google Trends(`pytrends`), Tavily Search API를 결합하여 해외 식품 박람회 참가를 준비하는 수출 기업에게 **정밀 키워드 도출, 왜곡 없는 시계열 트렌드 시각화, 통계 기반 계절성 판별 및 차기 시즌 B2B 소싱 전략, 현지 유통 매대 실존 경쟁사 및 뉴스 브리핑**을 제공하는 Flask 기반 웹 애플리케이션 명세서입니다.

본 문서는 소스 코드 없이도 시스템의 아키텍처, 입출력 데이터 규격, 프롬프트 엔지니어링 룰, 세부 알고리즘 및 UI/UX 동작 방식을 완벽히 재현하고 구현할 수 있도록 상세하게 기술되었습니다.

---

## 📌 1. 시스템 핵심 기능 및 비즈니스 로직

### 1.1 전 식품군 확장 6대 명세 입력 기반 분석
* **목적:** 스낵뿐만 아니라 음료, 소스/장류, HMR, 건기식 등 전 식품군에 대해 주관적인 미사여구로 인한 **GIGO(Garbage In, Garbage Out) 현상을 차단**합니다.
* **수집 명세 (6대 입력 항목 + 박람회 개최 월):**
  1. `product_name`: 출품 제품명 (예: 현미 누룽지칩, 궁중 약과, 떡볶이)
  2. `country`: 진출 대상 국가 (예: 미국, 벨기에, 독일, 말레이시아)
  3. `ingredients`: 핵심 베이스 원재료 (예: 현미 85%, 천일염, 쌀, 고추장)
  4. `target_price`: 목표 소매 가격대 및 단위 중량 (예: 4~5 USD / 150g)
  5. `certifications`: 보유 인증 (예: HACCP, 비건, 글루텐 프리, 할랄)
  6. `strengths`: 제품 물리적 식감/가공 메커니즘/맛 (예: 얇고 바삭한 스냅감, 유탕 처리를 배제한 고온 압착 로스팅)
  * `exhibition_month`: 참가 예정 박람회 개최 월 (기본값: "10월")

---

### 1.2 체급 왜곡 방지 3단 키워드 프레임워크 (OpenAI GPT-4o-mini)
* **목적:** 현지 인지도가 낮은 한국 식품이 '스낵', '음식' 같은 거대 일반명사와 묶여 검색량이 0으로 눌리는 현상을 원천 방지하고, 동등 체급의 로컬 대체재를 발굴합니다.
* **도출되는 3대 키워드 규격:**
  * **키워드 1 (현지 실질 표기명):** 타깃 국가 소비자가 구글에 실제로 입력하는 외래어/알파벳 표기 (예: `brown rice nurungji chips`, `yakgwa`).
  * **키워드 2 (직속 마이크로 카테고리):** [출신국/문화권] + [직속 카테고리/제형] (예: `Korean rice snacks`, `Korean glazed pastry`).
  * **키워드 3 (동등 체급 로컬 대체재):** 현지 시장에서 원재료, 물리적 식감/물성, 취식 상황, 가격대 체급이 가장 일치하는 **현지 고유의 대등 라이벌 제품 단어** (예: 미국의 `baked rice crackers`, 말레이시아의 `kuih`).
* **키워드 3 매핑 시 네거티브 룰 (금지 규칙):**
  * 원재료 명칭만 같고 물리적 식감 형태가 전혀 다른 제품 매핑 절대 금지 (예: 유과의 대체재로 쫄깃한 떡 '모찌(Mochi)' 매핑 금지 ➔ 바삭한 튀김 쌀과자 매핑).

---

### 1.3 이중 차트 뷰 모드 및 데이터 수집 알고리즘 (`pytrends`)
* **데이터 수집 파라미터:** `timeframe="today 12-m"` (최근 1년), 언어=`en-US`, 표준시간대=`360`. 타깃 국가의 ISO 2자리 국가코드(`geo_code`)로 1차 질의 후 데이터 부재 시 글로벌(`geo=""`)로 폴백 질의.
* **서버 내부 이중 정규화 계산 로직:**
  * **상대 비교 모드 (Relative Series):** 구글 트렌드가 기본 반환하는 키워드 간 실제 검색 점유율 (0~100 스케일, 체급 비교용).
  * **개별 트렌드 모드 (Independent Series - 기본 뷰):** 각 키워드별 연간 시계열 데이터 중 **자체 최고점(Max Value)을 100으로 잡고 백분율로 재정규화(`round((v / max_v) * 100)`)**하여, 체급 차이에 상관없이 각 키워드의 고유한 시즌성 곡선과 피크 시점을 복원.
* **API 장애 대응 Fallback:** Google 429(Rate Limit) 등 예외 발생 시 최근 12개월간의 시뮬레이션 데이터를 공급하고 `is_simulated=True` 플래그를 세팅하여 LLM 환각을 차단.

---

### 1.4 통계 기반 계절성 자동 판별 및 B2B 소싱 사이클 전략 (핵심 개선)
기존의 "모든 식품이 피크를 타며, 3~6개월 전 역산하라"는 획일적 오류와 뻔한 클리셰(건강 관심, SNS 바이럴 등)를 원천 차단하기 위해 **통계적 계절성 진단 및 시점 보정 로직**을 적용합니다.

#### ① 통계 기반 제품 특성 자동 분류 (Seasonal vs Stable)
* **판별 알고리즘:**
  * 메인 키워드의 연간 시계열 데이터에서 `최고점 / 연평균` 비율과 `검색량 0점 구간 비율`을 계산.
  * **시즌성 급등 품목 (Seasonal):** 피크 비율이 평균의 2.8배 이상이고 0점 구간이 유의미한 경우.
  * **연중 상시 소비재 (Year-round Stable):** 떡볶이, 라면, 김치, 간장처럼 연중 고른 수요를 형성하는 품목.
* **분기에 따른 전략 차별화:**
  * **상시 소비재:** 억지 피크 역산을 폐기하고, 바이어에게 **"시즌 리스크 없는 연중 안정적 회전율(Year-round High Turnover)"** 및 **"일상 필수 식재료(Pantry Staple)"** 관점의 부스 슬로건을 제시.
  * **시즌성 품목:** 피크 집중 월을 기준으로 3~6개월 전 B2B 사전 발주(Shelf-Ready) 슬로건을 도출.

#### ② 과거 시계열의 미래 시점 오인 교정 및 차기 시즌(Next Season) 연도 자동 계산
* **문제 해결:** 과거 12개월 실적 데이터(`2026-02-15` 등)를 두고 "앞으로 2월에 집중될 것으로 예상된다"거나 이미 지난 명절을 준비하라는 시점 왜곡을 방지합니다.
* **동적 연도 연산 메커니즘:**
  * 과거 데이터에서 피크가 발생했던 **'월(Month)'**만 분리 추출 (예: 2월).
  * **현재 시점(오늘의 Year, Month)**과 비교하여, 피크 월이 이미 지났다면 타깃 연도를 **`현재 연도 + 1`**로 자동 승격.
  * *예시:* 현재가 9월이고 과거 피크가 2월인 경우 ➔ **"다가오는 차기 시즌(내년 2월 설날/춘절)"**을 준비하기 위해 이번 가을 박람회(10~11월)에서 바이어 사전 샘플링을 진행해야 한다는 실전 무역 논리를 확립.

#### ③ 프롬프트 고정 템플릿(환각) 제거
* 프롬프트 내부에서 '건강에 대한 관심 증가', 'K-컬처/SNS 레시피 바이럴', '글루텐프리' 등의 Few-shot 예시 가이드를 전면 삭제하여, 데이터 패턴 판별 결과(`pattern_desc`)에 따른 고유한 B2B 분석 문장만 생성되도록 통제합니다.

---

### 1.5 현지 매대 경쟁사 및 식문화 팩트체크 뉴스 분석
* **매대 실존 경쟁사 (OpenAI GPT-4o):** 타깃 국가 유통 매대에 실제로 입점된 3개 이상의 대표 브랜드, 대표 제품 1종(제조사, 한국어 특징), 유사 식감 대체재 1종, 현지 평균 소매 가격대(현지 통화 및 원화 병기), 시장 진입 핵심 요약.
* **현지 식문화 뉴스 (Tavily Search API):** `advanced` 검색 깊이로 현지 최신 언론 보도를 수집하고 OpenAI로 한국어 요약문 작성 및 원문 팩트체크 URL 노출.

---

## 📂 2. 프로젝트 폴더 구조 (`app/google trend`)

```text
app/
└── google trend/
    ├── .env                       # API 인증키 및 모델 설정 파일
    ├── .gitignore                 # 환경변수, 캐시 DB, pycache 제외 설정
    ├── run.py                     # Flask 진입점 (포트 5050, 6대 명세 라우팅)
    ├── google_trends.py           # 키워드 추출, pytrends 수집, 계절성 진단 및 B2B 인사이트
    ├── competitors.py             # 현지 매대 실존 경쟁사 및 평균 가격대 분석
    ├── tavily_news.py             # Tavily 기반 현지 식품 트렌드 뉴스 수집 및 요약
    ├── readme.md                  # 본 시스템 기술 명세서
    └── templates/
        └── google_trends.html     # 반응형 6대 명세 입력 폼, 이중 차트 뷰, 인사이트 카드 UI